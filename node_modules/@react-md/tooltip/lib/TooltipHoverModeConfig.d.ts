import { ReactElement, ReactNode } from "react";
interface TooltipHoverModeConfigProps {
    children: ReactNode;
    enabled?: boolean;
    delayTimeout?: number;
    defaultDelay?: number;
}
/**
 * This component is used so that tooltips can gain the "hover mode"
 * functionality in that once a tooltip has become visible by hover, all other
 * tooltips will become visible immediately until 3 seconds have passed.
 */
declare function TooltipHoverModeConfig({ defaultDelay, delayTimeout, enabled, children, }: TooltipHoverModeConfigProps): ReactElement;
declare namespace TooltipHoverModeConfig {
    var propTypes: {
        enabled: any;
        delayTimeout: any;
        defaultDelay: any;
        children: any;
    };
}
export default TooltipHoverModeConfig;
