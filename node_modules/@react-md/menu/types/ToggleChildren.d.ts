import { ReactElement, ReactNode } from "react";
export interface ToggleChildrenProps {
    children?: ReactNode;
    visible: boolean;
    dropdownIcon: ReactNode;
    disableDropdownIcon: boolean;
}
declare function ToggleChildren({ dropdownIcon, disableDropdownIcon, children, visible, }: ToggleChildrenProps): ReactElement;
export default ToggleChildren;
