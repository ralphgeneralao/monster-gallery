import { ReactElement, ReactNode } from "react";
export interface PanelGroupProps {
    children?: ReactNode;
    persistent: boolean;
    disableTransition: boolean;
}
/**
 * @private
 */
export default function PanelGroup({ persistent, disableTransition, children, }: PanelGroupProps): ReactElement;
